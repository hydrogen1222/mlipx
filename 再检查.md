Session 1：工程与架构

审查：

配置优先级；
CLI/API/INCAR 一致性；
Calculator 参数传递；
Batch Queue 状态机；
多环境子进程；
GPU 锁；
原子写入；
路径安全；
resume/retry；
异常是否被吞掉。
Session 2：MD 实现

审查：

ASE 单位；
时间步单位；
Langevin friction 单位；
温度和自由度；
速度初始化；
质心平动移除；
约束原子；
NVE 能量守恒；
checkpoint 是否保存 momenta；
Langevin RNG 状态恢复；
pre-relax 是否改变原始结构；
guard 是否会误报或漏报。
Session 3：扩散和电导率算法

这是最重要的一轮，只让 Sol 或 Opus 做。

逐项核查：

固定晶胞分数坐标展开；
非正交晶胞矩阵方向；
minimum-image 修正；
连续两帧位移超过半个晶胞时的失效条件；
多时间原点 MSD；
原子平均与时间平均；
D=slope/6；
分方向 D
α
	​

=slope/2；
Å²/fs 到 m²/s 的换算；
载流子数量和数密度；
Nq
2
D/(Vk
B
	​

T)；
S/m 到 S/cm；
实际温度还是目标温度；
Haven ratio 未修正的标注；
拟合窗口；
ballistic、caging 和 diffusive 区分；
block uncertainty；
框架熔化检测；
不足扩散时是否拒绝报出可靠电导率。
Session 4：测试是否真的能证明正确

让模型不要继续看实现，而是从外部设计 oracle：

构造解析可知的人工轨迹；
构造跨 PBC 的直线运动；
构造各向异性随机游走；
构造静止原子；
构造整体漂移；
构造带 FixAtoms 的体系；
连续 NVE 与 50+restart+50 步对比；
轨迹截断和损坏；
队列中途杀进程后恢复；
两个控制器争抢同一 GPU；
缺少模型文件和环境时的启动前失败。

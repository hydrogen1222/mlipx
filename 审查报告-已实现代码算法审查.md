# 已实现代码理论计算原理与算法正确性审查报告

> 审查依据：`再检查.md` Session 1-4 逐项清单
> 审查范围：仅针对当前已实现代码，不涉及未实现功能
> 审查日期：2025-07-16

---

## 一、审查总览

| 分类 | 数量 | 说明 |
|------|------|------|
| 🔴 确认算法/理论错误 | 2 | 会导致错误数值结果 |
| 🟡 确认逻辑缺陷 | 4 | 边界条件下可能出错或结果被污染 |
| ⚠️ 设计缺陷 | 4 | 结构性问题，不立即触发但降低可靠性 |
| ✅ 已确认正确 | 10+ | 核心计算流程正确 |

**总体评价**：已有代码的核心计算流程（MD 积分、温度初始化、ASE 单位转换、配置解析）在数学和物理上是正确的。问题主要集中在：不可变数据被意外修改、边界检测的粒度和阈值、异常处理的鲁棒性、以及离线分析工具的单位/索引计算。

---

## 二、已确认正确的部分 ✅

### 2.1 MD Runner — ASE 单位转换

**文件**：`uma/mlipx/runners/md.py`，`MDRunner.__init__`

```python
self.timestep = timestep * units.fs   # fs → ASE 内部时间单位
self.friction = friction / units.fs   # 1/fs → 1/ASE-time
```

- `units.fs ≈ 0.09823`：1 fs = 0.09823 ASE 时间单位
- `timestep=1.0` → ASE 值 `0.09823` ✅
- `friction=0.001` → ASE 值 `0.01018` ✅
- 日志输出时正确用 `/units.fs` 还原 fs 显示 ✅

### 2.2 MD Runner — 速度初始化

**文件**：`uma/mlipx/runners/md.py`，`MDRunner._initialize_velocities`

```python
MaxwellBoltzmannDistribution(
    atoms,
    temperature_K=self.temperature,   # 使用现代 API，单位 K
    force_temp=True,                  # 强制精确温度
)
Stationary(atoms, preserve_temperature=True)  # 除 COM 平移 + 恢复温度
```

调用链验证：
1. `MaxwellBoltzmannDistribution` 生成 Maxwell-Boltzmann 分布的动量
2. `force_temp=True` 调用 `force_temperature` 将动能精确缩放到目标温度
3. `Stationary` 移除质心平动速度（`p0 / mtot`）
4. `preserve_temperature=True` 再次调用 `force_temperature(atoms, temp0)` 恢复温度
5. 最终效果：温度 = 目标值，Σ momenta = 0 ✅

测试验证（`tests/core/mlipx/test_md_runner.py`）：
```python
assert atoms.get_temperature() == pytest.approx(900.0)
assert np.sum(atoms.get_momenta(), axis=0) == pytest.approx(np.zeros(3), abs=1e-12)
```

### 2.3 MD Runner — 温度计算

**文件**：`uma/mlipx/runners/md.py`，`MDRunner._calculate_temperature`

```python
def _calculate_temperature(self, atoms):
    try:
        return atoms.get_temperature()
    except NotImplementedError:
        ndof = max(3 * len(atoms) - 3, 1)
        return 2 * ke / (ndof * units.kB)
```

- 主路径使用 ASE 的 `get_temperature()`，其 DOF = `3N - Σ(constraint.get_removed_dof())`
- 这与 `MaxwellBoltzmannDistribution` / Langevin thermostat 内部使用的 DOF 一致 ✅
- 回退路径 (`3N - 3`) 针对不报告 removed DOF 的约束（如 FixSymmetry）并发出警告

### 2.4 NVE MD Runner — 温度初始化（尽管使用 deprecated API）

**文件**：`src/fairchem/core/components/calculate/_single/nve_md_runner.py`

```python
MaxwellBoltzmannDistribution(atoms, temp * units.kB)
# temp = 1543.75 K
```

调用链验证：

| 步骤 | 表达式 | 值 |
|------|--------|-----|
| 输入 | `temp = 1543.75` K | — |
| 传递值 | `temp * units.kB` | `0.13303` eV |
| `process_temperature(temp=0.13303, temperature_K=None, 'eV')` | `0.13303 / kB` | `1543.75` K |
| `MaxwellBoltzmannDistribution` 内部 | `kB * 1543.75` | `0.13303` eV |
| 传递给 `_maxwellboltzmanndistribution` | `0.13303` eV = kB·T | ✅ 正确 |

虽然使用了 deprecated API（会发出 `FutureWarning`），但数学上完全正确。

### 2.5 配置解析 — 优先级合并

**文件**：`uma/mlipx/config/resolver.py`

6 层优先级，逐层合并，每层记录 `ResolvedValue(source=...)`：
```
built-in defaults < settings.ini < model alias < profile < INCAR/job < CLI/kwargs
```
- `calc_type` 从非权威层正确剔除（`layer.pop("calc_type", None)`）
- 空值 (`None` / `""`) 不覆盖已有值 ✅
- `explain()` 接口可追踪任意 key 的来源 ✅

### 2.6 GPU 兼容性 — 架构匹配算法

**文件**：`uma/mlipx/gpu_compat.py`

```python
def arch_supports_device(gpu_cc: str, arch_list: Sequence[str]) -> bool:
    gpu_major, gpu_minor = _parse_sm(gpu_cc)
    for token in arch_list:
        arch_major, arch_minor = _parse_sm(token)
        if arch_major == gpu_major and arch_minor <= gpu_minor:
            return True
    return gpu_cc in arch_list  # 回退精确匹配
```

核心逻辑 `arch_major == gpu_major and arch_minor <= gpu_minor` — 同主版本内，小版本向后二进制兼容。例如 sm_60 的 kernel 可在 sm_61 设备上运行。✅ 正确实现。

### 2.7 Job Manager — 原子状态写入

**文件**：`uma/mlipx/jobs.py`

```python
with tempfile.NamedTemporaryFile(mode="w", dir=self.jobs_dir,
    prefix=f".{job_id}.", suffix=".tmp", delete=False) as handle:
    json.dump(data, handle, indent=2)
    handle.flush()
    temp_path = Path(handle.name)
temp_path.replace(job_path)   # os.replace = 原子 rename
```

- 写入临时文件 → flush → 原子 replace ✅
- finally 块清理残留 `.tmp` 文件 ✅

### 2.8 Calculator Factory — 引擎选项分离

**文件**：`uma/mlipx/calculators/factory.py`

- `_CALC_KEYS` 按引擎定义各自的合法选项
- `_check_unknown_kwargs` 对跨引擎遗留选项发出 warning 并丢弃
- 真正未知的 key 提供 `difflib.get_close_matches` 建议 ✅

### 2.9 Schema 验证

**文件**：`uma/mlipx/config/schema.py`

- `OptionSpec.coerce()` 类型强转 + 范围检查 ✅
- `OptionSpec.matches()` 大小写不敏感匹配 ✅
- `Schema.suggest()` 对未知 key 提供相似建议 ✅

### 2.10 NVEMDReducer — moving_avg 实现

**文件**：`src/fairchem/core/components/benchmark/_single/nvemd_reducer.py`

```python
def moving_avg(x, window=20):
    all_x = []
    for i in range(window):
        all_x.append(x[i : len(x) - (window - i)])
    return np.stack(all_x).mean(axis=0)
```

对于长度为 N 的输入，生成 20 个长度为 N-20 的切片（每个切片偏移 1），然后取平均。这是标准的居中滑动平均实现，结果长度 N-20。✅ 正确。

---

## 三、确认的理论/算法错误 🔴

### Bug 1：MDRunner pre-relax 会变异调用者的 Atoms 对象

**文件**：`uma/mlipx/runners/md.py`，`MDRunner.run()`

**严重程度**：🔴 高 — 数据完整性破坏

```python
def run(self, atoms: Atoms) -> dict[str, Any]:
    # ❌ 没有 copy：atoms 是调用者传入的同一对象
    atoms = self._prepare_atoms(atoms)          # 原地修改 pbc, cell, info
    ...
    if self.pre_relax:
        atoms = self._pre_relax_structure(atoms) # FIRE 优化原地修改位置！
    ...
    self._initialize_velocities(atoms)           # 原地修改动量
```

`_pre_relax_structure` 内部：
```python
def _pre_relax_structure(self, atoms):
    atoms.calc = self._get_calculator()
    optimizer = FIRE(atoms, logfile=None)
    optimizer.run(fmax=self.pre_relax_fmax, steps=self.pre_relax_steps)
    return atoms  # ← 返回的是调用者传入的同一对象
```

**后果**：
- 调用者的 `Atoms` 对象的原子位置在 `run()` 后被永久改变
- 对同一 `Atoms` 连续两次 `run()` 产生不同结果（第二次从已松弛位置开始）
- 调用者无任何途径获知结构已变

**修复**：在 `run()` 开头加 `atoms = atoms.copy()`

---

### Bug 2：NVEMDReducer 能量漂移计算使用错误索引和单位

**文件**：`src/fairchem/core/components/benchmark/_single/nvemd_reducer.py`，`get_te_drift()`

**严重程度**：🔴 高 — 离线分析指标数值错误

```python
def get_te_drift(filename):
    time, Et = get_thermo(filename)
    total_step = len(time)
    equil_time = int(total_step * 0.1)          # 例如 N=2000 → equil_time=200
    Et = Et[equil_time:]                        # 新长度 = 1800
    total_time = time[-1] - time[equil_time]    # ✓ 时间跨度正确
    Et = moving_avg(Et, window=20)              # 新长度 = 1780
    drift = 1000 * np.abs(Et[-1] - Et[equil_time]) / total_time  # ❌❌❌
    return float(drift)
```

**错误 2a — 索引错误**：`Et[equil_time]` 即 `Et[200]` 在已截断+平滑后的数组中索引第 200 个元素，而非第一个平滑值。正确写法为 `Et[0]`。当前代码比较的是平滑数组中第 200 个元素与最后一个元素，得到的不是"从平衡后开始的漂移"。

**错误 2b — 缺少原子数归一化**：函数返回值中 `metric["drift_mev_per_atom_ps"]` 命名暗示每原子每皮秒的 meV 漂移，但 `get_te_drift` 未除以 `natoms`。

**错误 2c — 缺少 fs→ps 转换**：`total_time` 单位为 fs。`1000 * eV / fs = keV/fs`，而非 `meV/ps`。需要 `1000 * eV / (total_time/1000) = eV/ps` 即 `1000 * eV * 1000 / total_time_fs`。

**修复**：
```python
drift = 1000 * 1000 * np.abs(Et[-1] - Et[0]) / (total_time * natoms)
# = meV / (ps · atom)
```

---

## 四、确认的逻辑缺陷 🟡

### Bug 3：NaN 力度检测延迟可达 99 步

**文件**：`uma/mlipx/runners/md.py`，`MDRunner.run()` 内的 `print_progress()`

```python
def print_progress():
    # 每步检查：只检查能量 NaN，不检查力！
    self._check_finite(atoms, pe, context=f"MD step {step}")
    #                                  ↑ forces 参数未传入，默认 None

    # 每 100 步才检查力 NaN
    if step > 0 and step % 100 == 0:
        forces_chk = atoms.get_forces()
        self._check_finite(atoms, pe, forces=forces_chk, ...)
```

`_check_finite` 的 `forces` 参数为 `None` 时跳过力检查。MLIP 模型可能在力中产生 NaN 而能量正常，这些 NaN 力会被 ASE 积分器累积到位置和速度中，最多传播 99 步后才被检测。

**修复**：将每步的 `_check_finite` 也传入 forces，或至少将力 NaN 检测间隔缩短到与 `save_interval` 一致。

---

### Bug 4：GPU 兼容性检查吞没异常

**文件**：`uma/mlipx/calculator.py`，`UMACalculator._check_gpu_compatibility()`

```python
def _check_gpu_compatibility(self) -> None:
    ...
    try:
        major, minor = torch.cuda.get_device_capability(0)
        gpu_cc = f"sm_{major}{minor}"
        arch_list = torch.cuda.get_arch_list()
        if not arch_supports_device(gpu_cc, arch_list):
            raise RuntimeError(...)
    except RuntimeError:
        raise        # ✓ 显式 raise 被传播
    except Exception:
        pass         # ❌ 所有其他异常被静默吞没！
```

如果 `torch.cuda.get_device_capability(0)` 因为 CUDA 未初始化、设备索引无效、驱动不匹配等抛出非 `RuntimeError` 的异常（如 `AssertionError`、`CUDAError`），检查被完全跳过。用户可能在 GPU 兼容性问题被掩盖的情况下继续运行，直到远端的 MD 计算才崩溃。

**修复**：至少记录 warning 而非静默忽略：
```python
except Exception as exc:
    warnings.warn(f"GPU compatibility check skipped: {exc}")
```

---

### Bug 5：`BaseRunner._prepare_atoms` 原地修改 atoms 对象

**文件**：`uma/mlipx/runners/base.py`

```python
def _prepare_atoms(self, atoms: Atoms) -> Atoms:
    ...
    atoms.pbc = True                    # ← 原地修改
    atoms.center(vacuum=6.0)            # ← 原地修改分子位置
    atoms.info.setdefault("charge", 0)  # ← 原地修改 info dict
    atoms.info.setdefault("spin", 1)    # ← 原地修改 info dict
    ...
    return atoms  # 返回同一个对象引用
```

没有 `atoms = atoms.copy()` 的保护。这是与 Bug 1 同一类问题——调用者传入的结构被静默改变。

**影响**：
- PBC 设置被改变（非周期性 → 周期性）
- 分子体系位置被 `center(vacuum=6.0)` 改变
- `info` dict 被注入默认值

---

### Bug 6：JobManager.update_status 读-改-写竞态

**文件**：`uma/mlipx/jobs.py`

```python
def update_status(self, job_id, status, *, error=None):
    data = self._read_job_state(job_id)       # ← 步骤 1：读取
    if data is None:
        return False
    self._write_job_state(                     # ← 步骤 2：写入
        job_id=job_id, ...,
        pid=data["pid"],                       # 使用步骤 1 的旧值
        progress=data.get("progress"),         # 使用步骤 1 的旧值
        ...
    )
```

`_read_job_state` 和 `_write_job_state` 之间没有锁保护。多进程场景下（如 job_worker 正在更新 progress，同时 CLI `mlipx jobs kill` 更新 status），后来的 `_write_job_state` 可能用旧数据覆盖新状态。

**修复**：使用文件锁（`fcntl.flock` / `portalocker`）保护 read-modify-write 序列。

---

## 五、设计缺陷 ⚠️

### 5.1 力的"爆炸"检测阈值过高

**文件**：`uma/mlipx/runners/md.py:358`

```python
max_force = np.max(np.linalg.norm(forces_chk, axis=1))
if max_force > 50:  # eV/Å
    self.log(f"⚠️ WARNING: Large forces detected ({max_force:.1f} eV/Å)")
```

- 50 eV/Å 意味着原子之间的力异常巨大，原子已经飞出多个键长
- 真实体系中，5-10 eV/Å 即属异常
- 应使用配置中的 `fmax_warn=5.0` 和 `fmax_abort=20.0` 阈值

### 5.2 NVE 模式无能量守恒监控

**文件**：`uma/mlipx/runners/md.py`

NVE（Velocity Verlet）模式下理应监控总能量漂移（`|E(t) - E(0)| / |E(0)|`），但当前代码：
- 只报告 `PE + KE`（不跟踪与初始值的差异）
- 不检查总能量守恒是否在合理范围内
- `NVEMDReducer` 中的漂移计算仅用于离线 benchmark，未集成到运行时

### 5.3 Safety guard 配置未集成到 MD runner

**文件**：`uma/mlipx/config/defaults.py` 定义了以下 safety 参数，但 `MDRunner` 完全不读取：

| 配置键 | 默认值 | 含义 | 集成状态 |
|--------|--------|------|----------|
| `minimum_distance_warn` | 0.8 Å | 最近邻距离警告 | ❌ 未集成 |
| `minimum_distance_abort` | 0.5 Å | 最近邻距离中止 | ❌ 未集成 |
| `temperature_factor_warn` | 2.0 × | 温度偏离警告倍数 | ❌ 未集成 |
| `temperature_factor_abort` | 3.0 × | 温度偏离中止倍数 | ❌ 未集成 |
| `temperature_violation_count` | 5 | 温度连续违规计数 | ❌ 未集成 |
| `abort_on_nan` | true | NaN 时中止 | ✓ 部分（仅能量） |

### 5.4 BatchRunner 对 MD 类型无有效错误消息

**文件**：`uma/mlipx/runners/batch.py:215`

```python
if self.calc_type == "sp":
    runner = SinglePointRunner(...)
elif self.calc_type == "opt":
    runner = OptimizationRunner(...)
else:
    raise NotImplementedError(
        f"Batch mode for {self.calc_type} not yet implemented"
    )
```

用户通过 batch 配置 MD 时，看到的是笼统的 "not yet implemented"，没有解释原因（MD 通常不需要 batch 模式）或替代方案。

---

## 六、补充发现

### 6.1 `ASELangevinUMARunner` 中变量命名误导

**文件**：`src/fairchem/core/components/dynamics/ase_md_runner.py`

```python
friction_ps_inv: float = 0.001,   # ← 变量名暗示 1/ps
...
friction=self.friction_ps_inv / units.fs,  # ← 但当作 1/fs 使用
```

默认值 `0.001` 在物理上对应 `0.001 fs⁻¹ = 1.0 ps⁻¹`。所以值是对的，但变量名 `friction_ps_inv` 暗示传入值的单位应为 1/ps。如果用户被变量名误导传入 `friction_ps_inv=1.0`（意为 1/ps），实际会被当作 `1.0 fs⁻¹ = 1000 ps⁻¹`，产生过度阻尼。

### 6.2 `_maxwellboltzmanndistribution` 的 temp 参数含义

ASE 源码（`ase/md/velocitydistribution.py`）：
```python
def _maxwellboltzmanndistribution(masses, temp, comm=world, rng=None):
    """...
    temp: float
        The temperature in electron volt.
    """
```

`temp` 以 eV 为单位，是 `kB × T` 的能量值。`MaxwellBoltzmannDistribution` 的包装函数正确地将 K 转换为 eV。✅

---

## 七、修复优先级建议

| 优先级 | Bug | 影响范围 | 修复难度 |
|--------|-----|----------|----------|
| **P0** | Bug 1：pre-relax 变异 atoms | 所有 MD 计算 | 低（加一行 `.copy()`） |
| **P0** | Bug 2：漂移计算索引/单位错误 | Benchmark 结果 | 低（改索引+公式） |
| **P1** | Bug 4：GPU 检查吞异常 | 所有 GPU 计算 | 低（加 warning） |
| **P1** | Bug 3：NaN 力延迟检测 | MD 轨迹质量 | 中（需评估性能影响） |
| **P1** | Bug 5：_prepare_atoms 变异 | 所有计算类型 | 低（加 `.copy()`） |
| **P2** | Bug 6：job 状态竞态 | 多进程 job 管理 | 中（需引入文件锁） |
| **P2** | 5.3：safety guard 未集成 | MD 安全性 | 中（需在 MD 循环中注入检测） |
| **P2** | 5.2：NVE 无能量监控 | NVE 模拟可靠性 | 低（加 drift 输出） |
| **P3** | 5.1：爆炸阈值过高 | MD 早期预警 | 低（改用配置值） |
| **P3** | 5.4：batch MD 错误消息 | 用户体验 | 低（改错误文本） |
| **P3** | 6.1：变量命名误导 | 代码可维护性 | 低（重命名参数） |

---

## 八、测试缺口（与再检查.md Session 4 的对照）

当前测试覆盖（`tests/core/mlipx/`）：

| 测试文件 | 覆盖内容 | 缺失 |
|----------|----------|------|
| `test_md_runner.py` | 仅速度初始化温度正确性 | 无 NVE/NVT 对比、无约束、无 checkpoint |
| `test_calculator.py` | 仅参数验证（不加载模型） | 无实际计算 |
| `test_engine.py` | 仅配置构造 | 无实际 run |
| `test_jobs.py` | JobManager 原子写入 | 无并发测试、无 kill 恢复 |
| `test_cli.py` | argparse 解析 | 无端到端流程 |

按再检查.md Session 4 逐项对照，**所有 oracle 测试均缺失**：构造已知轨迹、跨 PBC 运动、各向异性游走、静止原子、整体漂移、FixAtoms、NVE 断点续跑、轨迹截断损坏、GPU 争抢、启动前失败检测。

---

*报告结束*

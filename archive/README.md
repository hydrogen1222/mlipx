# Historical archives

Everything below `archive/` is preserved only as historical development
reference. It is outside the installable `mlipx` package, is unsupported, and
must not be imported by live code or included in the normal CI/test matrix.
